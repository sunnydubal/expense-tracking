
  # Expense Tracker

  This is a code bundle for Expense Tracker. The original project is available at https://www.figma.com/design/fScTDhCkM5BpnfE555mmvZ/Expense-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  